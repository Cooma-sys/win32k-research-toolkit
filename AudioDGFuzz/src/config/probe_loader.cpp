// probe_loader.cpp — implementation is header-only (YAML parser inline)
// This file exists for build-system consistency with W32KFuzz pattern.
// All logic lives in probe_loader.hpp.
#include "config/probe_loader.hpp"

// No additional implementation needed — ProbeLoader is fully defined in header
// as a static-method-only class with inline helpers.
